/* let darPartida = false;

const partida = (ligar) => (ligar === true) ? 'on' : 'off';

console.log(`motor = ${partida(darPartida)}`); */


/* let raio = 2;
const areaCircle = (raioCircle) => (Math.PI * (raioCircle ** 2));

console.log(areaCircle(raio)); */

/* const frase = 'A maior palavra é Itaquaquecetuba'

const maiorPalavra = (fraseRef) => {
  const array = fraseRef.split(' ');
  let palavra = array[0];
  for (index = 0; index < array.length; index += 1) {
    if (array[index].length > palavra.length) {
      palavra = array[index];
    }                                                                                         
  }
  return palavra;
};

console.log(maiorPalavra(frase));
 */